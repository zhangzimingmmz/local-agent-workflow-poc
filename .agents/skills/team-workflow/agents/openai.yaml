interface:
  display_name: "Team Workflow"
  short_description: "Split and run tracked multi-role Git work"
  default_prompt: "Use $team-workflow to claim, split when needed, and complete my assigned Work Item."
policy:
  allow_implicit_invocation: false
