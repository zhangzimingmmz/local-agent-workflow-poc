interface:
  display_name: "Team Workflow"
  short_description: "Run a tracked multi-role Git workflow"
  default_prompt: "Use $team-workflow to claim and complete my assigned workflow work item."
policy:
  allow_implicit_invocation: false
